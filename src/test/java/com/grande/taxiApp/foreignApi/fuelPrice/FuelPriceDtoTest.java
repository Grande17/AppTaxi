package com.grande.taxiApp.foreignApi.fuelPrice;

import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.extension.ExtendWith;
import org.springframework.test.context.junit.jupiter.SpringExtension;

import static org.junit.jupiter.api.Assertions.*;

@ExtendWith(SpringExtension.class)
class FuelPriceDtoTest {

     /*
    FuelPriceDto fuelPriceDto;
    @BeforeEach
    void setUp() {
        fuelPriceDto = new FuelPriceDto("test","test","test");
    }

    @Test
    void getCurrency() {
        String get = fuelPriceDto.getCurrency();
        assertEquals(fuelPriceDto.getCurrency(),get);
    }

    @Test
    void getPrice() {
        String get = fuelPriceDto.getPrice();
        assertEquals(fuelPriceDto.getPrice(),get);
    }

    @Test
    void getCountry() {
        String get = fuelPriceDto.getCountry();
        assertEquals(fuelPriceDto.getCountry(),get);
    }

      */
}